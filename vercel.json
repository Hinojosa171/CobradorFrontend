{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-builds",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    {
      "src": "^/(?!api).*",
      "dest": "/index.html"
    }
  ]
}
